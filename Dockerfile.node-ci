FROM node:20

# Install dependencies
RUN apt-get update && apt-get install -y \
    git \
    python3 \
    jq \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

# Install global dependencies if needed (e.g. yarn is usually included in node images)
# RUN npm install -g yarn

CMD ["bash"]
